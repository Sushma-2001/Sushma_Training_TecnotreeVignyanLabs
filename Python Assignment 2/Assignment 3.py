#take the input from the user
strings = input("Enter a list of strings:").split()

#sort the string according to alphabetical order
sorted_strings = sorted(strings)

#print the sorted string
print(sorted_strings)
